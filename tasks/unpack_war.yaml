---
- name: Unapck xwiki WAR
  unarchive:
    src: "/tmp/{{xwiki_archive}}"
    dest: "{{xwiki_webapp}}"
    remote_src: yes
    owner: "{{ xwiki_tomcat_user }}"
    group: "{{ xwiki_tomcat_group }}"
